// Generated by `wit-bindgen` 0.47.0. DO NOT EDIT!

///|
/// creates a new empty container
pub fn create_container(name : String) -> Result[@container.Container, String] {
  let ptr = @ffi.str2ptr(name)
  let return_area = @ffi.malloc(12)
  wasmImportCreateContainer(ptr, name.length(), return_area)
  let lifted = match @ffi.load8_u(return_area + 0) {
    0 =>
      Result::Ok(@container.Container::Container(@ffi.load32(return_area + 4)))
    1 => {
      let result = @ffi.ptr2str(
        @ffi.load32(return_area + 4),
        @ffi.load32(return_area + 8),
      )
      Result::Err(result)
    }
    _ => panic()
  }
  @ffi.free(ptr)
  @ffi.free(return_area)
  return lifted
}

///|
/// retrieves a container by name
pub fn get_container(name : String) -> Result[@container.Container, String] {
  let ptr = @ffi.str2ptr(name)
  let return_area = @ffi.malloc(12)
  wasmImportGetContainer(ptr, name.length(), return_area)
  let lifted = match @ffi.load8_u(return_area + 0) {
    0 =>
      Result::Ok(@container.Container::Container(@ffi.load32(return_area + 4)))
    1 => {
      let result = @ffi.ptr2str(
        @ffi.load32(return_area + 4),
        @ffi.load32(return_area + 8),
      )
      Result::Err(result)
    }
    _ => panic()
  }
  @ffi.free(ptr)
  @ffi.free(return_area)
  return lifted
}

///|
/// deletes a container and all objects within it
pub fn delete_container(name : String) -> Result[Unit, String] {
  let ptr = @ffi.str2ptr(name)
  let return_area = @ffi.malloc(12)
  wasmImportDeleteContainer(ptr, name.length(), return_area)
  let lifted = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(())
    1 => {
      let result = @ffi.ptr2str(
        @ffi.load32(return_area + 4),
        @ffi.load32(return_area + 8),
      )
      Result::Err(result)
    }
    _ => panic()
  }
  @ffi.free(ptr)
  @ffi.free(return_area)
  return lifted
}

///|
/// returns true if the container exists
pub fn container_exists(name : String) -> Result[Bool, String] {
  let ptr = @ffi.str2ptr(name)
  let return_area = @ffi.malloc(12)
  wasmImportContainerExists(ptr, name.length(), return_area)
  let lifted = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(@ffi.load8_u(return_area + 4) != 0)
    1 => {
      let result = @ffi.ptr2str(
        @ffi.load32(return_area + 4),
        @ffi.load32(return_area + 8),
      )
      Result::Err(result)
    }
    _ => panic()
  }
  @ffi.free(ptr)
  @ffi.free(return_area)
  return lifted
}

///|
/// copies (duplicates) an object, to the same or a different container.
/// returns an error if the target container does not exist.
/// overwrites destination object if it already existed.
pub fn copy_object(
  src : @types.ObjectId,
  dest : @types.ObjectId,
) -> Result[Unit, String] {
  let ptr = @ffi.str2ptr(src.container)
  let ptr0 = @ffi.str2ptr(src.object)
  let ptr1 = @ffi.str2ptr(dest.container)
  let ptr2 = @ffi.str2ptr(dest.object)
  let return_area = @ffi.malloc(12)
  wasmImportCopyObject(
    ptr,
    src.container.length(),
    ptr0,
    src.object.length(),
    ptr1,
    dest.container.length(),
    ptr2,
    dest.object.length(),
    return_area,
  )
  let lifted = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(())
    1 => {
      let result = @ffi.ptr2str(
        @ffi.load32(return_area + 4),
        @ffi.load32(return_area + 8),
      )
      Result::Err(result)
    }
    _ => panic()
  }
  @ffi.free(ptr)
  @ffi.free(ptr0)
  @ffi.free(ptr1)
  @ffi.free(ptr2)
  @ffi.free(return_area)
  return lifted
}

///|
/// moves or renames an object, to the same or a different container
/// returns an error if the destination container does not exist.
/// overwrites destination object if it already existed.
pub fn move_object(
  src : @types.ObjectId,
  dest : @types.ObjectId,
) -> Result[Unit, String] {
  let ptr = @ffi.str2ptr(src.container)
  let ptr0 = @ffi.str2ptr(src.object)
  let ptr1 = @ffi.str2ptr(dest.container)
  let ptr2 = @ffi.str2ptr(dest.object)
  let return_area = @ffi.malloc(12)
  wasmImportMoveObject(
    ptr,
    src.container.length(),
    ptr0,
    src.object.length(),
    ptr1,
    dest.container.length(),
    ptr2,
    dest.object.length(),
    return_area,
  )
  let lifted = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(())
    1 => {
      let result = @ffi.ptr2str(
        @ffi.load32(return_area + 4),
        @ffi.load32(return_area + 8),
      )
      Result::Err(result)
    }
    _ => panic()
  }
  @ffi.free(ptr)
  @ffi.free(ptr0)
  @ffi.free(ptr1)
  @ffi.free(ptr2)
  @ffi.free(return_area)
  return lifted
}
